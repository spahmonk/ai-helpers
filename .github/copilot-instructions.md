# Copilot Instructions

## High-level architecture

- The repository is currently a minimal bootstrap repository: the only tracked project file is `README.md` at the root.
- There is no application source tree, package manifest, build pipeline, or test suite yet, so do not assume a language, framework, or runtime without first checking newly added files.
- `README.md` is currently the only in-repo project documentation and should be treated as the starting point for project context.

## Key conventions

- Keep changes aligned with the repository's current minimal structure; introduce new tooling or project scaffolding only when the task explicitly requires it.
- Before suggesting commands or architecture-specific changes, inspect the actual files present in the repository rather than inferring a stack from the repository name.
